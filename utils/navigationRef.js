import { createNavigationContainerRef } from '@react-navigation/native';

// Module-level ref assigned to NavigationContainer in App.js.
// Import this anywhere you need to navigate outside of a React component.
export const navigationRef = createNavigationContainerRef();
